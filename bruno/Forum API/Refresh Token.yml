info:
  name: Refresh Token
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:1984/api/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
